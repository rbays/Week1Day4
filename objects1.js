var outputDiv = document.getElementById("output"),
	myObject = {
	height : 12,
	width : 10,
	depth : 15
	};
outputDiv.innerHTML = "my object has height " + myObject.height + ", width " + myObject.width + " and depth " + myObject.depth;
